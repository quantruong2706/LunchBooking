export type User = {
  address: string
  age: string
  bankAccount: string
  name: string
  password: string
  phone: string
}

export type PayHistory = {}

export type Event = {
  address: string
  date: string
  name: string
  total: number
  userId: string
}
